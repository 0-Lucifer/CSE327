import 'package:flutter/material.dart';

class TrainHomePage extends StatelessWidget {
  const TrainHomePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Train Home'),
      ),
      body: Container(),
    );
  }
}