import 'package:flutter/material.dart';

class TravelGuidesScreen extends StatelessWidget {
  const TravelGuidesScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Travel Guides'),
      ),
      body: Container(),
    );
  }
}