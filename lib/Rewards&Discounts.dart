import 'package:flutter/material.dart';

class RewardsDiscountsScreen extends StatelessWidget {
  const RewardsDiscountsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Rewards & Discounts'),
      ),
      body: Container(),
    );
  }
}